services:
  - type: web
    name: stampbox
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python customer_web/server.py
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
